<span class="banner-logo" role="img" aria-label="Logo">
  <a href="/">Cassidy Smith</a>
</span>

<style lang="scss"> 
  @import '../styles/mixins.scss';

  .banner-logo {
    display: block;
    font-family: var(--font-heading);
    font-weight: 720;
    font-size: var(--font-size-1);
    line-height: var(--line-height-compressed);
    text-transform: uppercase;
    letter-spacing: 0.05ch;

    a {
      border-radius: var(--radius-1);
      padding-left: var(--fluid-bento-padding);
      padding-right: var(--fluid-bento-padding);
      padding-top: calc(var(--fluid-bento-padding) / 2);
      padding-bottom: calc(var(--fluid-bento-padding) / 2);
      display: block;
      text-decoration: none;
      transition: background-color 0.2s, color 0.2s, text-decoration-color 0.2s;


      &:link, &:visited {
        color: white;
        background-color: var(--color-primary-600);
      }
      &:hover {
        background-color: var(--color-primary-525);
      }
      &:active, &:hover:active {
        background-color: var(--color-accent-one-525);
      }

      &:focus {
        @include focus-outline;
      }

    }
  }
</style>